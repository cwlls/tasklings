/**
 * Tasklings -- app.js
 * Main application JavaScript module.
 * Expanded in Phase 9 (UI polish) and Phase 10 (offline support).
 */

// HTMX global configuration
document.addEventListener("DOMContentLoaded", () => {
    // Configure htmx defaults here as needed
});
